# This is a basic workflow that is manually triggered

name: CI

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
    # Triggers the workflow on push or pull request events but only for the main branch
  push: 
    branches: [ main ]
  #pull_request:
    #branches: [ main ]
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'warning'
      tags:
          description: 'Test scenario tags'
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
 build:
  runs-on: windows-latest
    # Steps represent a sequence of tasks that will be executed as part of the job
  steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Run a one-line script
      run: mvn clean install
      continue-on-error: true
    - name: Uplaod artifacts
      uses: actions/upload-artifact@v2
      with:
        name: TestReport
        path: test-output/myReport.html
    - run: mkdir deployment && cp target/* .war deployment
    - run: pwd && Is && Is target/* && Is deployment/*
    - run: C:\Users\SawantKumar\.jenkins\workspace\GitMaven\target\surefire-reports
    
     


      
